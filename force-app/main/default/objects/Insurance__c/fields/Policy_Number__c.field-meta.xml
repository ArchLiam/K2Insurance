<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Policy_Number__c</fullName>
    <description>Returns the actual policy number by removing the appended status from the Name field.</description>
    <externalId>false</externalId>
    <formula>IF(
    CONTAINS(Name, &quot; (&quot;),
    LEFT(Name, FIND(&quot; (&quot;, Name) - 1),
    Name
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Displays the policy number portion of the record Name before the status suffix.</inlineHelpText>
    <label>Policy Number</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
