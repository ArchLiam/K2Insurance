<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description>Durable exception event. Published (PublishImmediately) from flow fault paths — via the shared Log_Error subflow — so the error is logged even when the triggering transaction rolls back (e.g. a Custom Error block). Subscribed by the Exception_Event_Logger flow, which persists an Error_Log__c row.</description>
    <eventType>HighVolume</eventType>
    <label>Exception</label>
    <pluralLabel>Exceptions</pluralLabel>
    <publishBehavior>PublishImmediately</publishBehavior>
</CustomObject>
